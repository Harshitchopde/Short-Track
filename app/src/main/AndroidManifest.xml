<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.ShortsTrack">


    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:usesCleartextTraffic="true"

        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Funjo"
        tools:targetApi="31">
        <activity
            android:name="com.example.ShortsTrack.LoginRegister.LoginActivity"
            android:theme="@style/Theme.Funjo.noActionbar"
            android:exported="false" />
        <activity
            android:name="com.example.ShortsTrack.LoginRegister.SignUpActivity"
            android:theme="@style/Theme.Funjo.noActionbar"
            android:exported="true" >

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="com.example.ShortsTrack.MainActivity"
            android:theme="@style/Theme.AppCompat.Reels"
            android:exported="false">

        </activity>
        <activity
            android:name="com.example.ShortsTrack.ProfileUpdate"
            android:theme="@style/Theme.AppCompat.Reels"
            android:exported="false">

        </activity>
    </application>

</manifest>